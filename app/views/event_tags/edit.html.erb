<h1>EventTags controller, edit action</h1>

<p>Edit this file in <tt>app/views/event_tags/edit.html.erb</tt></p>

<%= error_messages_for :event_tag %>

<% form_for(@event_tag, :action => url(:event_tag, @event_tag)) do %>
  <p>
    <%= submit_button "Update" %>
  </p>
<% end %>

<%= link_to 'Show', url(:event_tag, @event_tag) %> |
<%= link_to 'Back', url(:event_tags) %>
