<h1>UserTags controller, new action</h1>

<p>Edit this file in <tt>app/views/user_tags/new.html.erb</tt></p>

<%= error_messages_for :user_tag %>

<% form_for(@user_tag, :action => url(:user_tags) ) do |f| %>
  <p>
    <%= submit_button "Create" %>
  </p>
<% end %>

<%= link_to 'Back', url(:user_tags) %>
