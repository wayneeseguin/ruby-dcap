<h1>UserTags controller, edit action</h1>

<p>Edit this file in <tt>app/views/user_tags/edit.html.erb</tt></p>

<%= error_messages_for :user_tag %>

<% form_for(@user_tag, :action => url(:user_tag, @user_tag)) do %>
  <p>
    <%= submit_button "Update" %>
  </p>
<% end %>

<%= link_to 'Show', url(:user_tag, @user_tag) %> |
<%= link_to 'Back', url(:user_tags) %>
