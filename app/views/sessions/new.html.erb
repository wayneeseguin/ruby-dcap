<h1>Sessions controller, new action</h1>

<p>Edit this file in <tt>app/views/sessions/new.html.erb</tt></p>

<%= error_messages_for :session %>

<% form_for(@session, :action => url(:sessions) ) do |f| %>
  <p>
    <%= submit_button "Create" %>
  </p>
<% end %>

<%= link_to 'Back', url(:sessions) %>
