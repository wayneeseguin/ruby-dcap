<h1>Events controller, index action</h1>

<p>Edit this file in <tt>app/views/events/index.html.erb</tt></p>

<table>
  <tr>
  </tr>

<% for event in @events %>
  <tr>
    <td><%= link_to 'Show', url(:event, event) %></td>
    <td><%= link_to 'Edit', url(:edit_event,  event) %></td>
    <td><%= delete_button :event, event %></td>
  </tr>
<% end %>
</table>

<%= link_to 'New', url(:new_event) %>
