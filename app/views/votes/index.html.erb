<h1>Votes controller, index action</h1>

<p>Edit this file in <tt>app/views/votes/index.html.erb</tt></p>

<table>
  <tr>
  </tr>

<% for vote in @votes %>
  <tr>
    <td><%= link_to 'Show', url(:vote, vote) %></td>
    <td><%= link_to 'Edit', url(:edit_vote,  vote) %></td>
    <td><%= delete_button :vote, vote %></td>
  </tr>
<% end %>
</table>

<%= link_to 'New', url(:new_vote) %>
