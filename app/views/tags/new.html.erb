<h1>Tags controller, new action</h1>

<p>Edit this file in <tt>app/views/tags/new.html.erb</tt></p>

<%= error_messages_for :tag %>

<% form_for(@tag, :action => url(:tags) ) do |f| %>
  <p>
    <%= submit_button "Create" %>
  </p>
<% end %>

<%= link_to 'Back', url(:tags) %>
